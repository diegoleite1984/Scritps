#  SCRIPT PARA EXTRAIR DADOS DOS PDF DENTRO DAS PASTAS (NOME-CPF-CARGO-ORGAO-CID)

import fitz  # PyMuPDF
import re
import pandas as pd
from pathlib import Path
from collections import defaultdict
import os

# --- Configurações ---
pasta_pdfs = r"Z:\DESSS\Apoio Tecnico\MODELOS 2020\APOSENTADORIA\servidores 2020"
nome_arquivo_saida = "2020.xlsx"

# Verificar se a pasta existe
pasta_path = Path(pasta_pdfs)
if not pasta_path.exists():
    print(f"Erro: A pasta '{pasta_pdfs}' não existe.")
    exit()

# Listar todos os PDFs na pasta
pdf_files = list(pasta_path.rglob("*.pdf"))
if not pdf_files:
    print("Nenhum PDF encontrado na pasta.")
    exit()

print(f"Encontrados {len(pdf_files)} arquivos PDF para processar.")

# Estrutura para armazenar os dados agrupados por pessoa
dados_agrupados = defaultdict(lambda: {"CID": set(), "Cargo": "", "Órgão": "", "Arquivos": []})

# Processar cada PDF
for pdf_file in pdf_files:
    print(f"Processando: {pdf_file.name}")

    try:
        with fitz.open(pdf_file) as doc:
            texto_extraido = ""
            for page in doc:
                texto_extraido += page.get_text()

        # --- Extração ---
        nomes = re.findall(r"Nome:\s*([^\n\r]+)", texto_extraido)
        cpfs = re.findall(r"\b\d{3}\.\d{3}\.\d{3}-\d{2}\b", texto_extraido)
        cids = re.findall(r"CID-10:\s*([A-Z0-9\.]+)", texto_extraido)
        cargos = re.findall(r"Cargo:\s*([^\n\r]+)", texto_extraido)
        orgaos = re.findall(r"Ó[rR][gG][ãaA][oO]:\s*([^\n\r]+)", texto_extraido)

        # Garantir que todas as listas tenham o mesmo tamanho
        max_len = max(len(nomes), len(cpfs), len(cargos), len(orgaos), 1)

        # Preencher listas menores com valores vazios
        nomes += [""] * (max_len - len(nomes))
        cpfs += [""] * (max_len - len(cpfs))
        cargos += [""] * (max_len - len(cargos))
        orgaos += [""] * (max_len - len(orgaos))

        # Agrupar dados
        for i in range(max_len):
            chave = (nomes[i].strip(), cpfs[i].strip())
            if chave[0]:  # Só adiciona se tiver nome
                dados_agrupados[chave]["CID"].update(cids)
                if cargos[i].strip():
                    dados_agrupados[chave]["Cargo"] = cargos[i].strip()
                if orgaos[i].strip():
                    dados_agrupados[chave]["Órgão"] = orgaos[i].strip()
                dados_agrupados[chave]["Arquivos"].append(pdf_file.name)

    except Exception as e:
        print(f"Erro ao processar {pdf_file.name}: {e}")

# Criar DataFrame final
linhas = []
max_cids = 0

for (nome, cpf), info in dados_agrupados.items():
    cids_lista = sorted(info["CID"])
    max_cids = max(max_cids, len(cids_lista))

    linha = {
        "Nome": nome,
        "CPF": cpf,
        "Cargo": info["Cargo"],
        "Órgão": info["Órgão"],
        "Arquivos": "; ".join(info["Arquivos"]),
        "Total_CIDs": len(cids_lista)
    }

    # Adicionar CIDs em colunas separadas
    for idx, cid in enumerate(cids_lista, start=1):
        linha[f"CID_{idx}"] = cid

    linhas.append(linha)

# Criar DataFrame
df = pd.DataFrame(linhas)

# Garantir que todas as colunas CID existam e estejam na ordem correta
colunas_cid = []
for i in range(1, max_cids + 1):
    coluna = f"CID_{i}"
    colunas_cid.append(coluna)
    if coluna not in df.columns:
        df[coluna] = ""

# Reordenar colunas
colunas_ordenadas = ["Nome", "CPF", "Cargo", "Órgão", "Total_CIDs", "Arquivos"] + colunas_cid
df = df[colunas_ordenadas]

# Mostrar preview dos dados no console
print(f"\nPreview dos dados no DataFrame:")
print(df.head().to_string(index=False))
print(f"\nShape do DataFrame: {df.shape}")

# Caminho completo do arquivo de saída
excel_path = pasta_path / nome_arquivo_saida

# Salvar Excel com configurações específicas
try:
    # Remover arquivo existente se houver
    if excel_path.exists():
        os.remove(excel_path)
        print("Arquivo existente removido.")

    # Salvar com engine xlsxwriter para melhor compatibilidade
    with pd.ExcelWriter(excel_path, engine='openpyxl') as writer:
        df.to_excel(writer, index=False, sheet_name='Resultados')

        # Ajustar largura das colunas automaticamente
        worksheet = writer.sheets['Resultados']
        for idx, col in enumerate(df.columns):
            max_length = max(df[col].astype(str).map(len).max(), len(col)) + 2
            worksheet.column_dimensions[chr(65 + idx)].width = min(max_length, 50)

    print(f"\n✅ Extração concluída com sucesso!")
    print(f"📊 Total de registros: {len(df)}")
    print(f"💾 Arquivo Excel salvo em: {excel_path}")

    # Verificar se o arquivo foi criado corretamente
    if excel_path.exists():
        print(f"📁 Tamanho do arquivo: {excel_path.stat().st_size} bytes")

        # Tentar ler o arquivo salvo para verificar
        try:
            df_verificacao = pd.read_excel(excel_path)
            print(f"✅ Arquivo lido com sucesso - {len(df_verificacao)} registros verificados")
            print(f"📋 Colunas no arquivo salvo: {list(df_verificacao.columns)}")
        except Exception as e:
            print(f"❌ Erro ao verificar arquivo salvo: {e}")
    else:
        print("❌ Erro: Arquivo Excel não foi criado.")

except Exception as e:
    print(f"❌ Erro ao salvar Excel: {e}")
    print("Tentando salvar com método alternativo...")

    # Método alternativo
    try:
        df.to_excel(excel_path, index=False)
        print("✅ Salvo com método alternativo.")
    except Exception as e2:
        print(f"❌ Erro no método alternativo: {e2}")

# Mostrar estatísticas finais
print(f"\n📈 Estatísticas finais:")
print(f"- Pessoas únicas encontradas: {len(df)}")
print(f"- Máximo de CIDs por pessoa: {max_cids}")
print(f"- Total de PDFs processados: {len(pdf_files)}")
print(df)
df.to_excel('teste.xlsx', index=False)
